#!/bin/bash
set -e

# check if we are in 
#echo "importing (default) database if needed"
#mysql -h${MARIADB_HOST} -uroot -p${MARIADB_ROOT_PASSWORD} ${MARIADB_DATABASE} < /root/resources/fogbackup.sql

# run required services
#apachectl
#php-fpm

# sleep...
while true
do
    sleep infinity
done
